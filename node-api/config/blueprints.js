module.exports.blueprints = {

  // Enable RESTful blueprint routes
  rest: true,

  // Enable shortcut routes (handy in dev)
  shortcuts: true,

  // Leave actions off since we're using controllers manually
  actions: false
};
